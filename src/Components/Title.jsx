function title({ children }) {
  return (
    <h1 className="text-3xl text-slate-100 font-bold text-center mb-6">
      {children}
    </h1>
  );
}
export default title;
// This Title component can be used to display the main title of a page or section
